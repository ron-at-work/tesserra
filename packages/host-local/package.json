{
  "name": "@agent-proof/host-local",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "default": "./dist/src/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run test --exclude \"**/dist/**\""
  },
  "dependencies": {
    "@agent-proof/service": "workspace:*",
    "@agent-proof/api-server": "workspace:*",
    "@agent-proof/crypto-local": "workspace:*",
    "@agent-proof/storage-sqlite": "workspace:*"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "@types/node": "catalog:",
    "vitest": "catalog:"
  }
}
